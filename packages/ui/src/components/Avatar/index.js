/**
 * Prism Avatar Component
 * 
 * A customizable avatar component built with Spectrum Web Components and Prism design tokens.
 * Supports multiple brands (Quest, Volt), themes (Light, Dark), and variants.
 */

// Export the component
export { default } from './Avatar.js';

// Re-export for convenience
export { default as PrismAvatar } from './Avatar.js';
