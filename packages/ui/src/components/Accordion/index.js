/**
 * Prism Accordion Component
 * 
 * A collapsible content component built with Spectrum Web Components and Prism design tokens.
 * Supports multiple brands (Quest, Volt), themes (Light, Dark), and content types.
 */

// Export the component
export { default } from './Accordion.js';

// Re-export for convenience
export { default as PrismAccordion } from './Accordion.js';

