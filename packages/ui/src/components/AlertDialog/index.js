/**
 * Prism Alert Dialog Component
 * 
 * A customizable alert dialog component built with Spectrum Web Components and Prism design tokens.
 * Supports multiple brands (Quest, Volt), themes (Light, Dark), and variants.
 */

// Export the component
export { default } from './AlertDialog.js';

// Re-export for convenience
export { default as PrismAlertDialog } from './AlertDialog.js';
