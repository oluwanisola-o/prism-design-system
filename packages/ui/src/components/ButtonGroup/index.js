/**
 * Prism Button Group Component
 * 
 * A customizable button group component built with Spectrum Web Components and Prism design tokens.
 * Supports multiple brands (Quest, Volt), themes (Light, Dark), and variants.
 */

// Export the component
export { default } from './ButtonGroup.js';

// Re-export for convenience
export { default as PrismButtonGroup } from './ButtonGroup.js';
